apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-consumer
  labels:
    app: order-consumer
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-consumer
  template:
    metadata:
      labels:
        app: order-consumer
    spec:
      containers:
        - name: order-consumer
          image: registry.internal/order-consumer:latest
          env:
            # Capped to the broker's advertised per-fetch limit on the new
            # cluster. Fewer orders per poll, but the poll itself is cheaper.
            - name: CONSUMER_BATCH_SIZE
              value: "50"
            - name: CONSUMER_POLL_COST
              value: "40ms"
            - name: CONSUMER_WORK_COST
              value: "100us"
            - name: CONSUMER_BACKLOG
              value: "5000"
